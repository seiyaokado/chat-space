.header
  .left-header
    .left-header__title
      = @group.name
    %ul.left-header__members
      Member：
      - @group.users.each do |user|
        = user.name
  .right-header
    .right-header__button
      Edit